// git hub で前に戻る方法を知る。
// git hub でブランチを切る方法を知る。
// コマンドラインで知る
import { Block } from "@/components/Block";

const Home: React.FC = () => {
  return <Block leftBlockCount={10} rightBlockCount={10} />;
};

export default Home;
